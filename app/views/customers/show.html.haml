%h1= @customer.name
%p= @customer.phone
%ul
  %li= link_to 'Add a Rental', new_customer_rental_path(@customer)
  %li= link_to "Destroy", customers_path(@customer), |
      method: :delete,                           |
      data: { confirm: "Are you sure?" }         |
